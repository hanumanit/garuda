# Run config for Qwen3.8-27B in Q4_K_M (~19 GB) on a 16 GB machine.
#
# A different shape of model from Mixtral: there are no experts to stream, but three
# quarters of its 64 blocks are gated delta nets — linear attention carrying a
# fixed-size recurrent state instead of a growing KV cache. What that changes is set
# out below, and in the README.
#
#   curl -L https://huggingface.co/ggml-org/Qwen3.8-27B-GGUF/resolve/main/Qwen3.8-27B-Q4_K_M.gguf \
#     -o Qwen3.8-27B-Q4_K_M.gguf
#
# `--config` goes before the subcommand: `garuda --config qwen3.8.toml serve`.

[model]
path = "./models"
gguf = "/path/to/Qwen3.8-27B-Q4_K_M.gguf"
# Essential: keep 19 GB packed on disk and dequantise per row. Expanding this
# checkpoint to f32 would want well over 100 GB of RAM.
mmap = true
# 19 GB against 16 GB of RAM, so a prompt token that traverses the model alone
# re-streams the whole file. Batching drops the working set to one block.
prefill_batch = 256
# Qwen3.8 was trained for 262 144 tokens. This caps the KV cache, not correctness —
# and only the 16 attention blocks store anything per position, so the cache is a
# quarter of what an all-attention model of this size would want.
context = 4096
# A small checkpoint that proposes tokens for this one to check. The natural draft for
# a Qwen3.5-family model is a smaller one from the same family — it is the only thing
# that shares the 248 320-token vocabulary, and startup refuses a mismatch.
#
# This is the single largest win available here, because it cuts *passes over the
# weights*, which is what a checkpoint larger than RAM actually spends its time on.
# Measured on the same 10-token reply: 152 s with no speculation, 108 s with prompt
# lookup (which needs no second model), and 58 s with a Qwen3.5-0.8B draft — 2.6x,
# with the output identical token for token.
#
#   draft_gguf = "/path/to/Qwen3.5-0.8B-Q8_0.gguf"
draft_gguf = ""
# Let the model reason. Its template opens a `<think>` block for the assistant;
# `false` closes it immediately, so a reply is the answer alone. `true` leaves it
# open, which is the template's own default — raise sampling.max_tokens if you do,
# because the reasoning arrives as content ahead of the answer.
thinking = false
gpu = false

[memory]
# Enough resident blocks to cover the 4096-token context (4096 / 32 = 128), so the
# attention blocks never spill and reload under full attention.
kv_resident_blocks = 128
# One cached prompt holds this sequence's whole recurrent state — about 144 MB for
# this checkpoint, whatever the prompt's length. A budget below that caches nothing
# at all, and Garuda says so at startup.
prompt_cache_entries = 4
prompt_cache = "1GB"
# Weights to hold in buffers this process owns rather than leaving to the page cache.
# Counter-intuitively, small is what pays here. Measured on this checkpoint, seconds
# per forward pass: 12.5 with none pinned, 11.8 at 2 GB, 13.7 at 6 GB, 25.8 at 9 GB.
# Owned memory competes with the page cache and, once the machine is under pressure,
# gets compressed or swapped — while a mapped weight is a clean page the kernel can
# drop for free and read back sequentially. 2 GB covers the output head, which every
# token reads in full, plus a block. Raising it much past that backfires.
weight_cache = "2GB"
kv_spill = false

[runtime]
# 0 uses every core, which is not always the fastest thing on a CPU whose cores are
# not alike. Measured on an 8+4 Apple Silicon machine, alternating runs so that page
# cache warming could not explain it: ~37 s per forward pass across all twelve cores
# against ~31 s across the eight performance cores, and steadier. A slow core holding
# up a batch costs more than it adds. Set this to your performance-core count.
threads = 0
# Warm the next blocks while this one computes. This is the setting that matters on a
# checkpoint larger than RAM: left to demand paging, the forward pass faults its
# weights in a page at a time and the CPU idles between faults — measured 140% busy
# out of 800% available, pulling 0.7 GB/s from a disk that reads 3.9 GB/s. Three
# background threads handing the kernel whole blocks took a forward pass from 27 s to
# 12 s on this machine, with the answer unchanged (the hint is advisory; the pass
# still faults whatever it needs).
prefetch = true
# Expert routing history, which a dense model does not have.
predictor = false

[server]
host = "127.0.0.1"
port = 8091
# One sequence at a time: paged inference over a checkpoint larger than RAM is
# memory-sensitive, and every token is another pass over 19 GB.
max_concurrent = 1
queue_capacity = 16
# The timeout has to cover the whole reply, and here a token costs ~24 s on a 16 GB
# machine: sampling.max_tokens below is 32, so the worst case is ~13 minutes and this
# leaves room for the prompt as well. Raise both together — a max_tokens the timeout
# cannot reach turns every long reply into "request timed out" after the work is done.
request_timeout_secs = 1800

[sampling]
temperature = 0.7
top_p = 0.95
top_k = 40
# ~24 s per token here, so this is about thirteen minutes of generation. It is also
# what the built-in chat page opens with, since it reads this server's defaults from
# /v1/stats rather than assuming a number a fast model would want.
max_tokens = 32
